#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin>>s;
    string ans="";
    stack<char>a;

    for(int i=0;i<s.size();i++){
        char ch=s[i];
        a.push(ch);
    }

    while(!a.empty()){
        ans+=a.top();
        a.pop();
    }

    cout<<ans<<endl;
}


